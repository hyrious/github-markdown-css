{
    // there's no typescript in this repo, the tsconfig is
    // only required for esbuild to resolve files
    "compilerOptions": {
        "paths": {
            "react": ["./shims/react.js"],
            "react-dom": ["./shims/react-dom.js"]
        }
    }
}
